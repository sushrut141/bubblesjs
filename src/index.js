import {Bubbles} from './bubbles';
import {
    map,
    filter,
    groupBy,
} from './data/operators';

export default {
    bubbles: Bubbles,
    transform: {
        map,
        filter,
        groupBy,
    },
};